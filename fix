if (column_type.startswith("I") or     # INTEGER-like types: I, I1, I2
    column_type == "F" or              # FLOAT
    column_type == "D" or              # DECIMAL
    column_type == "N" or              # NUMERIC
    column_type == "BF" or             # BYTE FLOAT (if exists)
    column_type == "RD"):              # REAL DOUBLE (if applicable)
